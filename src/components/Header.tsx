import { FC } from "react";

const Header: FC = () => {
  return (
    <header>
      <div className="bg-cover bg-center bg-gradient-to-r from-blue-400 via--500 to-green-500">
      </div>
    </header>
  );
};

export default Header